#!/usr/bin/env node

// TODO: Can we get some tab completion for this?
// Allow for loading of .yml files
var yaml = require('js-yaml');

// Load in the presentation
var CliPresentation = require('../');
CliPresentation.cli().parse(process.argv);
